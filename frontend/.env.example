VITE_API_BASE_URL=http://localhost:8000
VITE_DEMO_MODE=true
# Shared mode overrides demo/local startup and requires a protected backend.
# The team access code is entered at login, NEVER placed in a VITE variable.
VITE_SHARED_MODE=false
VITE_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
